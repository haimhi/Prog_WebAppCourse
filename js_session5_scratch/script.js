/* let total = 0;
const products = { grogger: 10, 'bottle of wine': 90, mask: 45 };
for (const [key, value] of Object.entries(products)) {
  let price = value;
  if (price >= 50) {
    price-=5;
  }
  console.log(key, price, 'nis', typeof key, typeof value, typeof products);
  total += value;
}
let totalWithTax = total * 1.17;
console.log('total', total, 'nis', typeof total);
console.log('total including tax', totalWithTax.toFixed(2), 'nis', typeof totalWithTax); */